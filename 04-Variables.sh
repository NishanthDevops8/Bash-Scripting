#!/bin/bash

#This file is to discuss the variables.

#What is variable ? Generally used to hold some value dynamically.
# There is no data type in the shell scripting by default everything is string.
# If your input or variable have some special characters it should be enclosed in the double quotation.
#Using the special characters are going to print the value of the variables.
# $ special character used to print the value of the variable
# echo $a
# $a = ${a}
# Variables are declared in the way
a=10
b=20
c=hell0
echo "Print $a"
echo "Print $b"
echo "Print $c"
echo " Day 19 session is completed "